/* let
    type lst = {val: string, next: lst}


    function printList (lst: lst) =
        if lst = nil then print("END\n")
        else (print(lst.val); printList(lst.next))

    var list3 := lst {val="how you?", next=nil}
    var list2 := lst {val="world", next=list3}
    var list1 := lst {val="hello", next=list2}
in
    printList(list1)
end */

/* let
    type tst = {val: string}

    function compare(x: tst, y: tst) = if x = nil then print("DONE\n") else print(y.val)

    var a := tst{val="hello"}
    var b := tst{val="world"}
in
    compare(a, b)
end */

/* let 
in 
    let 
        type rectype={}
        var a:=5 
    in 
    end; 5
end */

let 
in 
    let 
        type rectype={}
        var a:=5
    in 
    end;
    if (2 = 3 | (5 < 10 & 7 > 2)) then 4 else 7
end